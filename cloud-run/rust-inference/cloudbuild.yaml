# ============================================================================
# Cloud Build - Rust ML Inference Service
# Deploys to Cloud Run with IAM Authentication
# ============================================================================

steps:
  # Step 1: Build Docker image
  - id: 'build-image'
    name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/rust-inference:$SHORT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/rust-inference:latest'
      - '.'
    dir: 'cloud-run/rust-inference'

  # Step 2: Push to Container Registry
  - id: 'push-image'
    name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/rust-inference:$SHORT_SHA'
    waitFor: ['build-image']

  - id: 'push-latest'
    name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/rust-inference:latest'
    waitFor: ['build-image']

  # Step 3: Deploy to Cloud Run
  - id: 'deploy'
    name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'rust-inference'
      - '--image=gcr.io/$PROJECT_ID/rust-inference:$SHORT_SHA'
      - '--region=asia-northeast3'
      - '--platform=managed'
      - '--allow-unauthenticated'         # 🌐 Public (내부 ML 전용, 민감 데이터 없음)
      - '--memory=256Mi'                 # Rust는 메모리 적게 사용
      - '--cpu=1'
      - '--min-instances=0'              # 💰 Scale to Zero
      - '--max-instances=3'
      - '--timeout=30s'                  # ML 추론은 빠름
      - '--concurrency=80'               # Rust는 높은 동시성 처리 가능
    waitFor: ['push-image']

# Build timeout (Rust 빌드는 시간 소요)
timeout: '900s'

# Required images
images:
  - 'gcr.io/$PROJECT_ID/rust-inference:$SHORT_SHA'
  - 'gcr.io/$PROJECT_ID/rust-inference:latest'

# Note: Region is hardcoded to asia-northeast3 in deploy step
