{
  "name": "rule-engine",
  "version": "2.0.0",
  "description": "Rule-based Response Engine - 2025-11-20 업데이트",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=ruleEngine",
    "deploy": "gcloud functions deploy rule-engine --runtime nodejs20 --trigger-http --allow-unauthenticated --memory=256MB --timeout=30s"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.2",
    "axios": "^1.7.7"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
