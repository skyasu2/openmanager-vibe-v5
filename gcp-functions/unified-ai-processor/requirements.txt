# 🤖 Unified AI Processor - Python Dependencies (Cloud Run Compatible)
# 통합 AI 처리 엔진 - 2025-12-09 업데이트 (미사용 라이브러리 제거)

# Web Framework (Cloud Run)
flask==3.0.3                        # Cloud Run / Flask App

# Core Libraries
numpy==1.26.4                       # 수치 계산, 배열 연산
scikit-learn==1.5.2                 # ML 알고리즘 (TF-IDF, LogisticRegression, KMeans)
pandas==2.2.3                       # 데이터 분석, DataFrame

# Networking & Caching (Required for Orchestration)
httpx==0.27.0                       # 비동기 HTTP 클라이언트
cachetools==5.3.3                   # TTL 캐시 (응답 캐싱)

# NLP (English Support) - Korean uses rule-based patterns
spacy==3.7.4                        # 영어 NLP (NER, 토큰화)
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1.tar.gz

# ML & Analytics (Time Series Forecasting)
statsmodels==0.14.4                 # 통계 모델, Holt-Winters 시계열 예측

# Monitoring
structlog==24.4.0                   # 구조화된 로깅

# ============================================================================
# REMOVED (2025-12-09) - 미사용 라이브러리 제거
# ============================================================================
# konlpy==0.6.0           # 미사용 - 규칙 기반 패턴 매칭으로 대체
# soynlp==0.0.493         # 미사용 - 한국어 텍스트 처리 불필요
# regex==2024.11.6        # 미사용 - 표준 라이브러리 re 사용
# orjson==3.10.10         # 미사용 - 표준 json 모듈 사용
# google-cloud-storage==2.18.2   # 미사용 - GCS 연동 없음
# google-cloud-pubsub==2.23.1    # 미사용 - Pub/Sub 연동 없음
