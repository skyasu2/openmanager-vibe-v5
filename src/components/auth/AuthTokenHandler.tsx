/**
 * 🔐 클라이언트 사이드 인증 상태 동기화
 *
 * 서버 사이드 PKCE 콜백 핸들러가 세션을 쿠키에 설정한 후,
 * 클라이언트에서 세션 상태를 동기화합니다.
 *
 * 2026-01-04: 서버 사이드 PKCE로 전환 - 토큰 처리는 route.ts에서 수행
 */

'use client';

export function AuthTokenHandler() {
  // 서버 사이드 PKCE 사용 시 클라이언트에서 별도 처리 불필요
  // @supabase/ssr의 createBrowserClient가 쿠키에서 자동으로 세션을 읽음
  return null;
}
