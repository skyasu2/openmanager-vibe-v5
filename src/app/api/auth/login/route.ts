import { NextRequest, NextResponse } from "next/server"; import { simpleAuthService } from "@/services/auth/SimpleAuthService"; export async function POST(request: NextRequest) { try { const body = await request.json(); const { username, password } = body; if (!username || !password) { return NextResponse.json({ success: false, error: "사용자명과 비밀번호를 입력해주세요." }, { status: 400 }); } const result = await simpleAuthService.login({ username, password }); if (result.success) { return NextResponse.json(result, { status: 200 }); } else { return NextResponse.json(result, { status: 401 }); } } catch (error) { console.error("로그인 API 오류:", error); return NextResponse.json({ success: false, error: "서버 오류가 발생했습니다." }, { status: 500 }); } }
