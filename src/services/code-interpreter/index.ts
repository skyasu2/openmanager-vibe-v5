/**
 * 🐍 Code Interpreter Module
 *
 * Browser-based Python execution using Pyodide (WebAssembly).
 * Zero server cost - runs entirely in user's browser.
 *
 * @version 1.0.0
 * @created 2025-12-18
 */

export type { ExecutionResult, PyodideServiceConfig } from './pyodide-service';
export { pyodideService } from './pyodide-service';
