#!/usr/bin/env bash

# Pre-commit Hook: Code Quality Check
# 목적: ESLint/Prettier 자동 수정 (staged files만)

set -e

# lint-staged: ESLint + Prettier (fast, staged files only)
echo "🔍 Running lint-staged (ESLint + Prettier)..."
npx lint-staged || {
  echo "❌ Linting/Formatting failed"
  echo "💡 Some files were auto-fixed. Please review and re-add them."
  exit 1
}

exit 0
