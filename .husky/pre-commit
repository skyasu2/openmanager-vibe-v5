#!/usr/bin/env bash

# Pre-commit Hook: Code Quality Check
# 목적: ESLint/Prettier 자동 수정 (staged files만)

set -e

# 문서/테스트 검증 (Soft Warning)
# 소스 변경 시 문서/테스트 업데이트 여부 확인
./.husky/pre-commit-doc-validation || true

# Biome: Check & Format (fast, staged files only)
# ⚠️ Temporarily disabled during migration to Biome v2.3.8
# TODO: Re-enable after fixing all lint warnings (46 errors, 83 warnings)
# echo "🔍 Running Biome (Check & Format)..."
# npx @biomejs/biome check --write --staged --no-errors-on-unmatched || {
#   echo "❌ Biome check failed"
#   exit 1
# }
echo "⏭️ Biome check temporarily disabled (migration in progress)"

exit 0
