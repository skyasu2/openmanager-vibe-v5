#!/usr/bin/env bash

echo "🔍 Pre-push: Type-safe + Build validation (~90초 예상)..."
START_TIME=$(date +%s)

# WSL Performance Check (POSIX-compatible syntax)
case "$PWD" in
  /mnt/*)
    echo "⚠️  WARNING: You are running this from the Windows filesystem (/mnt/...)."
    echo "   This will be significantly slower (10x-50x) than running from the Linux filesystem (~/)."
    echo "   Consider moving your project to your WSL home directory."
    ;;
esac

# 1. 빠른 유닛 테스트 (11초) - CRITICAL (opt-out with SKIP_TESTS=true)
echo "🧪 Running quick tests..."
TEST_STATUS="passed"
if [ "${SKIP_TESTS:-false}" = "true" ]; then
  TEST_STATUS="skipped"
  echo "⚪ Tests skipped (SKIP_TESTS=true)"
  echo "⚠️  WARNING: Skipping tests may allow regressions to slip through"
else
  # Rollup 바이너리 체크 (WSL/Linux 환경)
  if command -v uname \u003e/dev/null 2\u003e\u00261 \u0026\u0026 [ "$(uname -s)" = "Linux" ]; then
    # Linux 환경에서는 바이너리 확인
    if ! npm list @rollup/rollup-linux-x64-gnu \u003e/dev/null 2\u003e\u00261; then
      echo "⚠️  Rollup Linux binary not found"
      echo "   This is a known issue in WSL environments"
      echo "   Skipping tests (build validation will still run)"
      TEST_STATUS="skipped-rollup"
    else
      npm run test:super-fast || {
        TEST_STATUS="failed"
        echo "❌ Tests failed - push blocked"
        echo ""
        echo "💡 Fix the test failures:"
        echo "   npm run test:super-fast"
        echo ""
        echo "⚠️  To bypass (not recommended):"
        echo "   SKIP_TESTS=true git push"
        exit 1
      }
    fi
  else
    # Windows/macOS에서는 테스트 실행 시도
    npm run test:super-fast || {
      # Rollup 에러인지 확인
      if npm run test:super-fast 2\u003e\u00261 | grep -q "@rollup"; then
        echo "⚠️  Rollup binary issue detected"
        echo "   Tests skipped (build validation will still run)"
        echo ""
        echo "ℹ️  To fix permanently:"
        echo "   - Windows: Install WSL and run from Linux"
        echo "   - Or use: SKIP_TESTS=true git push"
        TEST_STATUS="skipped-rollup"
      else
        TEST_STATUS="failed"
        echo "❌ Tests failed - push blocked"
        echo ""
        echo "💡 Fix the test failures:"
        echo "   npm run test:super-fast"
        echo ""
        echo "⚠️  To bypass (not recommended):"
        echo "   SKIP_TESTS=true git push"
        exit 1
      fi
    }
  fi
fi

# 2. 프로덕션 빌드 검증 (CRITICAL - Vercel 배포 전 로컬 검증, 타입 체크 포함)
# 2. 프로덕션 빌드 검증 (Optimized for Dev Speed)
# 기본적으로 Type Check + Lint만 병렬로 수행 (빠름)
# FULL_CHECK=true 설정 시에만 실제 빌드 수행 (느림)
echo "🏗️ Build validation..."
if [ "${SKIP_BUILD:-false}" = "true" ]; then
  echo "⚪ Build validation skipped (SKIP_BUILD=true)"
elif [ "${QUICK_PUSH:-false}" = "true" ]; then
  echo "⚡ Running optimized validation (Test + TypeScript parallel)..."
  # 테스트와 타입체크 병렬 실행 (QUICK_PUSH 최적화)
  VALIDATE_PID=""
  TEST_BG_STATUS=0

  # 타입체크 백그라운드 실행
  ./scripts/dev/validate-parallel.sh &
  VALIDATE_PID=$!

  # 테스트가 이미 실행되지 않았으면 병렬로 실행 (테스트 스킵 시 생략)
  if [ "$TEST_STATUS" = "passed" ] || [ "$TEST_STATUS" = "skipped" ] || [ "$TEST_STATUS" = "skipped-rollup" ]; then
    # 테스트는 이미 위에서 완료됨
    :
  fi

  # 타입체크 완료 대기
  wait $VALIDATE_PID || {
    echo "❌ Validation failed - push blocked"
    echo "💡 Fix errors or run 'npm run build' locally to debug"
    exit 1
  }
  echo "ℹ️  Note: Full build skipped (QUICK_PUSH=true). This is faster but less thorough."
else
  echo "🐢 Running FULL build validation (Default)..."
  echo "   (Use QUICK_PUSH=true git push to skip full build and run only Type Check + Lint)"
  npm run build || {
    echo "❌ Build failed - push blocked"
    exit 1
  }
fi

# 3. 환경 변수 검사 (CRITICAL - 배포 안전성)
echo "🔐 Environment variables check..."
npm run env:check || {
  echo "❌ Environment variables check failed - push blocked"
  echo "💡 Fix: Add missing env vars to .env.local"
  echo "⚠️  CRITICAL: Missing env vars will break deployment"
  echo "   (Check .env.example for required variables)"
  exit 1
}

# 4. package.json 검증
echo "📦 Checking package.json..."
node -e "JSON.parse(require('fs').readFileSync('package.json', 'utf8'))" || {
  echo "❌ package.json has syntax errors"
  exit 1
}

END_TIME=$(date +%s)
DURATION=$((END_TIME - START_TIME))

echo ""
echo "✅ Pre-push validation passed in ${DURATION}s"
echo "🚀 Ready to push - Type-safe & Build-safe guaranteed!"
echo ""
echo "📊 Validation Summary:"
# Test 상태 동적 표시
if [ "$TEST_STATUS" = "passed" ]; then
  echo "  ✅ Quick tests passed"
elif [ "$TEST_STATUS" = "skipped" ]; then
  echo "  ⚪ Quick tests skipped (SKIP_TESTS=true)"
elif [ "$TEST_STATUS" = "skipped-rollup" ]; then
  echo "  ⚪ Quick tests skipped (Rollup binary issue)"
else
  echo "  ❌ Quick tests failed (should not reach here)"
fi
echo "  ✅ Production build succeeded (includes type check)"
echo "  ✅ Environment variables validated"
echo "  ✅ package.json validated"
echo ""
echo "ℹ️  Post-commit (background, non-blocking):"
echo "  • ESLint → logs/lint-reports/"
echo "  • TypeScript → logs/typecheck-reports/"
echo "  • AI review → logs/code-reviews/"
echo ""
echo "🛡️  Protection: Type errors + Build failures blocked at local"
echo "💡 Vercel deployment will be safe"
