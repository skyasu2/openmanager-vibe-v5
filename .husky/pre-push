echo "🔍 Pre-push: Type-safe + Build validation (~90초 예상)..."
START_TIME=$(date +%s)

# 1. 빠른 유닛 테스트 (11초) - TEMPORARILY OPTIONAL
echo "🧪 Running quick tests..."
TEST_STATUS="passed"
npm run test:super-fast || {
  TEST_STATUS="failed"
  echo "⚠️  Tests failed (known Rollup native binary issue)"
  echo "💡 TODO: Fix @rollup/rollup-linux-x64-gnu installation"
  echo "ℹ️  Production build validation (step #3) is still enforced"
  echo "   Continuing with remaining validations..."
}

# 2. Lint 검사 (수정된 파일만 - 빠른 검증)
echo "🔍 Linting changed files..."
# origin/main과 비교하여 실제 변경된 파일 체크 (fallback: HEAD~1)
CHANGED_FILES=$(git diff origin/main...HEAD --name-only --diff-filter=ACM 2>/dev/null || git diff HEAD~1 --name-only --diff-filter=ACM 2>/dev/null || true)
CHANGED_FILES=$(echo "$CHANGED_FILES" | grep -E '\.(ts|tsx|js|jsx)$' || true)
LINT_STATUS="passed"
if [ -n "$CHANGED_FILES" ]; then
  echo "$CHANGED_FILES" | xargs npx eslint --max-warnings=0 || {
    LINT_STATUS="failed"
    echo "⚠️  Lint warnings/errors found in changed files"
    echo "💡 Fix: npm run lint -- --fix"
    echo "ℹ️  Continuing with remaining validations..."
  }
else
  echo "ℹ️  No TypeScript/JavaScript files changed, skipping lint"
  LINT_STATUS="skipped"
fi

# 3. TypeScript 타입 체크 (CRITICAL - 프로덕션 안전성)
echo "🔍 Type checking..."
npm run type-check || {
  echo "❌ TypeScript errors found - push blocked"
  echo "💡 Fix: npm run type-check"
  echo "⚠️  CRITICAL: Type errors must be fixed before push"
  echo "   (Vercel may deploy broken code if this is skipped)"
  exit 1
}

# 4. 프로덕션 빌드 검증 (CRITICAL - Vercel 배포 전 로컬 검증)
echo "🏗️ Build validation..."
npm run build || {
  echo "❌ Build failed - push blocked"
  echo "💡 Fix: npm run build"
  echo "⚠️  CRITICAL: Build must succeed before push"
  echo "   (Prevents Vercel deployment failures)"
  exit 1
}

# 5. 환경 변수 검사 (CRITICAL - 배포 안전성)
echo "🔐 Environment variables check..."
npm run env:check || {
  echo "❌ Environment variables check failed - push blocked"
  echo "💡 Fix: Add missing env vars to .env.local"
  echo "⚠️  CRITICAL: Missing env vars will break deployment"
  echo "   (Check .env.example for required variables)"
  exit 1
}

# 6. 민감 파일 체크 (보안)
echo "🔒 Security check..."
if git diff origin/main...HEAD --name-only 2>/dev/null | grep -E '\.(env|key|pem)$'; then
  echo "❌ 민감한 파일이 포함되어 있습니다"
  exit 1
fi

# 7. package.json 검증
echo "📦 Checking package.json..."
node -e "JSON.parse(require('fs').readFileSync('package.json', 'utf8'))" || {
  echo "❌ package.json has syntax errors"
  exit 1
}

END_TIME=$(date +%s)
DURATION=$((END_TIME - START_TIME))

echo ""
echo "✅ Pre-push validation passed in ${DURATION}s"
echo "🚀 Ready to push - Type-safe & Build-safe guaranteed!"
echo ""
echo "📊 Validation Summary:"
# Test 상태 동적 표시
if [ "$TEST_STATUS" = "passed" ]; then
  echo "  ✅ Quick tests passed"
else
  echo "  ⚠️  Quick tests failed (non-blocking, known issue)"
fi
# Lint 상태 동적 표시
if [ "$LINT_STATUS" = "passed" ]; then
  echo "  ✅ Lint check passed (changed files only)"
elif [ "$LINT_STATUS" = "skipped" ]; then
  echo "  ⚪ Lint check skipped (no files changed)"
else
  echo "  ⚠️  Lint check failed (warnings found, non-blocking)"
fi
echo "  ✅ TypeScript type check passed"
echo "  ✅ Production build succeeded"
echo "  ✅ Environment variables validated"
echo "  ✅ Security check passed"
echo "  ✅ package.json validated"
echo ""
echo "🛡️  Protection: Type errors + Build failures blocked at local"
echo "💡 Vercel deployment will be safe"
