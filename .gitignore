# Dependencies
node_modules/
venv/
.venv/
.venv-wsl/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
*.log

# npm cache (--cache .npm 옵션으로 생성, CI 재사용용)
.npm/

# Testing
/coverage
/test-results/
.lighthouseci/
/playwright-report/
**/test-results/
**/playwright-report/
**/unit-test-results/

# Playwright
.playwright-mcp/
**/traces/
dashboard-after-*
dashboard-before-*
*-test*.png
openmanager-*.png
playwright-*.png
/step*.png
qa*.png
mobile-*.png
gradient-verify-*.png
test-output/
screenshots/
!docs/screenshots/
docs/screenshots/*
!docs/screenshots/demo/
# Visual regression snapshots (generated by tests)
tests/e2e/visual-regression.spec.ts-snapshots/

# Playwright MCP / Chrome DevTools MCP text snapshots (루트 유출 방지)
# browser_snapshot 도구가 CWD에 생성하는 accessibility tree .md 덤프
*-snapshot.md
qa-*.md
# 단, 추적해야 할 문서 예외
!docs/**/*.md
!reports/**/*.md
!.claude/**/*.md

# Temporary files
*.log
test-*.txt
# Playwright/QA 실행 결과 임시 파일
landing-production-*.txt
landing-*.txt
validation-production-*.txt
artifacts/
# Deterministic test source paths that use the artifacts domain name.
!tests/artifacts/
tests/artifacts/*
!tests/artifacts/intent-classifier.bench.ts
!tests/fixtures/artifacts/
tests/fixtures/artifacts/*
!tests/fixtures/artifacts/intent-corpus.ts
.temp-*
# Root-level build/deploy/test logs (moved to logs/)
build_*.txt
deploy_*.txt
install_*.txt
install_*.log
*_log.txt
*_debug.txt
verification.log
setup.log
build-before.log
dev.log
test-results.json
playwright-test.png
bundle-before.txt
project-structure.txt
sloc_report.txt

# Next.js
/.next/
/out/
/build


# System files
.DS_Store
*.pem

# Backup directories
.backups/
backups/

# Environment files (unified)
.env*
!.env.example
!.env.grafana.example

# Cache and debug
.cache/
.npm-fix-trash/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 🔐 보안 및 민감한 정보 방지

# API 키 및 토큰이 포함될 수 있는 파일들
**/secrets.*
**/api-keys.*
**/tokens.*
**/*secret*
**/*token*
**/*credential*

# But allow API route source files (not actual tokens)
!src/app/api/auth/revoke-github-token/
!src/app/api/dev/get-test-token/

# Allow pre-commit secret scanner scripts (they detect secrets, not contain them)
!scripts/env/*secret*.cjs
!scripts/env/*secret*.js

# 테스트 파일 (API 키가 포함될 수 있음)
test-gemini*.js
test-*-direct.js
test-*-lite.js

# CSRF 토큰 API는 예외 (보안 로직이므로 git에 포함)
!src/app/api/csrf-token/

# Token usage tracker는 예외 (실제 토큰이 아닌 사용량 추적 코드)
!cloud-run/ai-engine/src/lib/token-usage-tracker.ts
!src/app/api/csrf-token/**

# 이슈 파일에서 실제 키가 노출된 경우 (패턴 기반)
**/*-issues-with-keys.md
**/*-api-keys-*.md
**/*-tokens-*.md

# 백업 파일에서 민감한 정보 방지
*.backup
*.bak
*.tmp
*_backup
*_bak
*.orig

# 개발자 개인 설정 (토큰 포함 가능)
.vscode/settings.json
.vscode/launch.json
.gitconfig
.claude/personal/
.claude/private/
# logs 디렉토리 (ai-decisions 제외)
**/logs/*
!logs/ai-decisions/
logs/ai-decisions/*
!logs/ai-decisions/*.md
!logs/ai-decisions/README.md
!logs/ai-decisions/TEMPLATE.md

dev.log
combined.log
ai-combined.log
# Codex CLI runtime (secrets, sessions, snapshots, tmp)
.codex/
# Gemini CLI runtime (auth, history, state)
.gemini/state.json
.gemini/history/
.gemini/tmp/
# Gemini-only project skills are trackable overlays.
!.gemini/skills/
!.gemini/skills/**
# temporary files
*~
*.tmp
*.temp


# 🔄 환경변수 자동 백업 파일들 (임시 백업 제외)
config/env-backups/
**/env-backup-*.json
env-backup-production-*.json
env-backup-development-*.json
config/env-backup.json
config/env-backup-latest.json
# Slack webhooks and sensitive URLs
slack-webhook-url.txt
webhook-urls.txt
*.webhook
*.slack

# 🔐 Production environment variables (contains sensitive data)
docs/environment-variables-production.md
**/environment-variables-production.md
production-env.md
sensitive-env.md

# 🔐 Secure token storage
.secure-tokens.json
.secure-tokens.json.backup*
.secure-tokens-export.sh

# 🔍 Tavily MCP 보안 파일
config/tavily-encrypted.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# cursor IDE configuration (contains sensitive data)
.cursor/

# 🔐 MCP 설정
# Claude Code의 project-scoped .mcp.json은 공식 공유 설정 파일로 사용하되,
# 시크릿은 env/wrapper로만 주입한다. 백업/로컬 변형은 계속 차단.
!.mcp.json
.mcp.json.backup*
*.mcp.json.backup
.claude/mcp.json
.claude/claude_workspace.json

# MCP 개발용 설정 파일들 (GitHub 허용, Vercel 배포 제외)
# cursor.mcp.json
# MCP-QUICK-FIX.md

# MCP 레거시 설정 파일들 (GitHub 차단)
mcp.json
cursor-mcp-config.json
corrected-cursor-mcp.json
*-mcp-config.json

# 🔐 Supabase MCP 보안 파일들 (Personal Access Token 포함)
.claude/supabase-mcp-config*.json
.claude/*.pat
.claude/*.token
.claude/*-secure.md

# Claude Code 로컬 설정 (사용자별 개인 설정)
.claude/settings.local.json

# AI Engine Context and Logs (not development artifacts)
logs/ai-context/
logs/ai-analysis/
logs/ai-tools-health/
data/vector-cache/
**/summary-intent-analysis.json
**/ai-context-*.json
**/vector-embeddings-*.json
mcp-memory/
.ai-cache/

# dev sandbox
temp-*
test2-*
debug-*
copy-*

# GCP 모니터링 로그 및 리포트
logs/gcp-*.json
reports/gcp-quota-report-*.md
.vscode/gcp-alerts.json

# AI 검토 보고서 및 분석 리포트
reports/*
!reports/README.md
!reports/docs/
!reports/docs/docs-inventory.md
!reports/planning/
!reports/history/
!reports/ai-review/
!reports/ai-review/**/
!reports/ai-review/**/.gitkeep
!reports/qa/
!reports/qa/**
reports/qa/runs/**/root-cleanup-*/
reports/qa/runs/**/*.png
reports/qa/runs/**/*.jpg
reports/qa/runs/**/*.jpeg
reports/qa/runs/**/*.webp
reports/qa/latest-qa-result.json
reports/history/*
!reports/history/README.md
!reports/history/legacy-scripts/
!reports/history/legacy-scripts/**
# AI 코드 리뷰 파일 (로컬 전용, GitHub 제외)
# - 모든 .md 파일은 추적 제외 (README.md 포함)
# - .gitkeep만 추적하여 폴더 구조 유지
reports/ai-review/**/*.md
reports/ai-review/*.md
reports/ai-review/.*

# External MCP servers (cloned from GitHub)
fetch-mcp/
fetch-mcp-server/

# 💾 커밋 백업 폴더 (롤백 시 생성되는 백업)
commit/

# 🔐 GitHub 인증 정보 (절대 커밋 금지)
.git-credentials
~/.git-credentials
*github_token*
*github_pat*
*.pat
.github-token

# GitHub 인증 정보
.github-auth.json

# Git rewrite 임시 파일
.git-rewrite/

# Claude 세션 정보
.claude_session.json

# Claude Code - 개인/보안 설정만 제외 (AI 검증 시스템은 팀 공유)
.claude/cache/
.claude/logs/
.claude/temp/
.claude/projects/
.claude/todos/
.claude/backup/
.claude/downloads/
.claude/shell-snapshots/
.claude/statsig/
.claude/*-local.json
.claude/user-preferences.json
.claude/.claude.json
.claude/.credentials.json
.claude/context.env
.claude/consolidation-*.log
.claude/hooks.log

# Archive folder (local temporary storage only)
archive/
!docs/archive/
!docs/archive/**

# 🛠️ Personal tools folder (WSL-First policy)
.local/

# 실행 파일들
*.exe

# GCP CLI
google-cloud-cli-linux-x86_64.tar.gz
google-cloud-sdk/

# 테스트 및 분석 결과 파일들
*-results.json
*-report.json
typescript-errors.log
COMMIT_MSG.tmp

# 정체불명 파일
50%

# Husky 설치 오류 잔재 디렉터리
--version/
disable/

# Windows/OS 잔재
AppData/

# 에이전트 임시 산출물
source_files.txt


# ESLint cache
.eslintcache
.next/cache/eslint/
eslint-report.json

# Backup files
*.backup
*.backup.*
src.backup.*
eslint.config.mjs.backup
scripts/fix-case-declarations.cjs

# Serena MCP
.serena/
.serena-mcp/

# 루트 디렉토리 문서 제한 (JBGE 정책에 따라 허용된 파일만)
/*.md
!README.md
!CLAUDE.md
!CHANGELOG.md
!CHANGELOG-LEGACY*.md
!GEMINI.md
!QWEN.md
!AGENTS.md
!KIRO.md

# AI 리뷰 폴더 README 제외 (위 !README.md 규칙 재정의)
reports/ai-review/README.md

# Scripts moved to /scripts/
enhanced-vm-control.ps1
ai-vm-interface.ps1
auth-now.ps1
auto-deploy.bat
fix-vm-now.sh
gcp-auth.bat
port-forward.ps1
quick-ssh-key-register.ps1
startup-script.sh
gcp-vm-manager.ps1
claude-wsl-optimized.bat
ai-cli-wsl.bat
setup-sudo-nopasswd.sh
execute-optimization.js

# Legacy config files (루트 디렉토리만 무시, config/ 폴더는 추적)
/next.config.performance.mjs
/next.config.optimized.mjs
/next.config.ultra-optimized.mjs
/package.optimized.json
/vitest.config.dom.ts
/vitest.config.minimal.ts
/vitest.config.node.ts
/vitest.config.performance.ts

# Reports moved to /reports/
gcp-functions-health-report.json

# TypeScript 임시 파일 (pre-commit hook에서 생성)
*.temp.json
.tsconfig.temp.json
tsconfig.*.temp.json

# 🏗️ Build & Test Results (Enhanced - 2025-08-20)
coverage/
playwright-report/
test-results/
cloud-run/ai-engine/results/
.nyc_output/
dist/
build/
.tmp/
tmp/

# 📊 Large Analysis Files (Enhanced - 2025-08-20)
reports/analysis/*.json
reports/performance/*.json
*.analysis.json
eslint-result.json
*-results.json
*-report.json

# 🔧 IDE & Tools (Enhanced - 2025-08-20)
.idea/
*.swp
*.swo
*~


# Screenshots and temporary images
*.screenshot.png
*.temp.png
# Numbered project screenshots (documentation captures)
[0-9][0-9]-*.png
[0-9][0-9][a-z]-*.png
# Playwright/dev verification screenshots (root level)
/*.png

# 🔒 보안: 디버그 로그 파일 Git 추적 제외
*.debug.log
*debug-hook.log
.claude/*.log
.claude/debug-*

# 📦 통합 아카이브/백업 시스템 (프로젝트 내부 관리)
# 아카이브 폴더들 (문서+코드+API)
archive/
!docs/archive/
!docs/archive/**
!supabase/archive/
!supabase/archive/**
!reports/planning/archive/
!reports/planning/archive/**
docs/.backup/
src/.backup/
src/app/api/.backup/
*/.backup/

# 압축 파일들 (필요시 생성)
*.tar.gz
archive/*.tar.gz
# AI 교차검증 히스토리
reports/verification-history/
!reports/verification-history/.gitkeep

# Multi-AI MCP 히스토리 (로그 수준 JSON 제외, 문서화는 docs에 저장)
packages/multi-ai-mcp/history/*.json
packages/multi-ai-mcp/history/**/*.json
!packages/multi-ai-mcp/history/.gitkeep

# 🚫 백업 파일들 (Git 추적 금지)
# GCP 함수 백업 (민감한 API 키 포함)
scripts/backup/
**/backup/
*.backup.json
**/gcp-functions-*/
**/gcp-backup-*/

# 일회성 분석 리포트 (reports/ 폴더로 이동 예정)
docs/gcp-analysis-report.md
docs/gcp-free-tier-analysis.md
docs/gcp-functions-deduplication-analysis.md
docs/rag-system-optimization-complete.md
docs/supabase-cleanup-guide.md

# Maintenance 문서 (Cron 자동화는 "필요 시 수동 실행" 정책과 상충)
docs/maintenance/

# PIN 인증 테스트 리포트 (임시 테스트 파일, 커밋 제외)
docs/testing/pin-authentication-*.md
**/pin-authentication-*.md
*.pid

# Runtime temp files
.dev-server.pid
dev-server.log
cloud-run/ai-engine/tmp-env.yaml

# Biome and linting reports (temporary analysis files)
biome_report*.txt
biome_report*.json
lint_*.txt
lint_*.json
type_check_report*.txt
tsc_output*.txt
tsc_*.log
build_*.txt
build_*.log
test_*.log
test_*.txt
vitest_*.log
.mcp.json.tmp

# 🔧 Maintenance & Analysis Output (임시 분석 결과, 루트 스코프)
/oldest_files*.json
/*-analysis-output.json
/cleanup-*.json
/maintenance-*.json

# 🛠️ One-off utility scripts (일회성 유틸리티, 루트 스코프)
/scripts/find-oldest-files.js
/scripts/maintenance/cleanup-manager.js
install_log.txt

# Cloud Run data (except hourly-data for SSOT sync)
cloud-run/*/data/
!cloud-run/ai-engine/data/
!cloud-run/ai-engine/data/hourly-data/
# Generated by preflight/deploy sync from public/data/otel-data (do not commit duplicate)
cloud-run/ai-engine/data/otel-data/
cloud-run/*/build*.log
cloud-run/*/*.log

# 🔐 Local Docker secrets (development only)
cloud-run/.env
cloud-run/ai-engine/.env
cloud-run/docker-compose.override.yml

# Cloud Run generated data
# Note: precomputed-states.json is now tracked (SSOT with hourly-data)

# Local npm global directory
.npm-global/

# Storybook
storybook-static/

# Planning tmp/ — local-only scratch files, not committed
reports/planning/tmp/

# msw가 install마다 재생성하는 브라우저 워커. 이 저장소는 node 서버 모드만 쓰고
# (setupWorker 참조 0건), 6a3b07542에서 orphan asset으로 한 번 정리한 적이 있다.
public/mockServiceWorker.js
