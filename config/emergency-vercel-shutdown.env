# 🚨 VERCEL PRO 사용량 위기 - 긴급 종료 설정
# 즉시 Vercel 환경변수에 추가하여 사용량을 99% 감소시킵니다

# === 1. 모든 Edge Runtime 비활성화 ===
DISABLE_ALL_EDGE_RUNTIME=true
FORCE_NODE_RUNTIME=true

# === 2. 모든 스케줄러 완전 비활성화 ===
EMERGENCY_THROTTLE=true
UNIFIED_METRICS_DISABLED=true
KEEP_ALIVE_SCHEDULER_DISABLED=true
SERVER_DATA_SCHEDULER_DISABLED=true
BACKGROUND_SCHEDULERS_DISABLED=true

# === 3. API 호출 최소화 ===
MAX_STATUS_REQUESTS_PER_MINUTE=5
SYSTEM_STATUS_POLLING_DISABLED=true  
HEALTH_CHECK_POLLING_DISABLED=true
AI_QUERY_DISABLED=true

# === 4. 클라이언트 폴링 완전 비활성화 ===
REACT_QUERY_REFETCH_INTERVAL=3600000
REACT_QUERY_DISABLED=true
AUTO_REFRESH_DISABLED=true

# === 5. 캐싱 최대한 활용 ===
VERCEL_EDGE_CACHE_ENABLED=true
VERCEL_CDN_CACHE_MAX_AGE=3600
FORCE_STATIC_GENERATION=true

# === 6. AI 기능 최소화 ===
GOOGLE_AI_DISABLED=true
AI_AGENT_DISABLED=true
MCP_DISABLED=true

# === 7. 데이터 생성 중단 ===
DATA_GENERATOR_DISABLED=true
METRICS_COLLECTION_DISABLED=true
MOCK_DATA_ONLY=true

# === 8. 로깅 최소화 ===
LOG_LEVEL=error
CONSOLE_LOG_DISABLED=true
ANALYTICS_DISABLED=true

# ⚡ 즉시 적용 방법:
# 1. Vercel 대시보드 → Settings → Environment Variables
# 2. 위 모든 변수를 Production에 추가
# 3. 재배포 (vercel --prod)
# 4. 5분 내 사용량 99% 감소 예상

# 📊 예상 효과:
# - Edge Request: 100K → 100 (99.9% 감소)
# - Function Invocations: 920K → 10K (98.9% 감소)  
# - 모든 자동 기능 비활성화
# - 정적 페이지만 서빙
# - Pro 사용량 위기 완전 해결 