# 🚨 응급 조치: Edge Request 사용량 제한 설정
# Vercel 환경변수에 다음 설정을 추가하세요:

# 서버 측 Rate Limiting
EMERGENCY_THROTTLE=true
MAX_STATUS_REQUESTS_PER_MINUTE=10
VERCEL_EDGE_CACHE_ENABLED=true
VERCEL_CDN_CACHE_MAX_AGE=300

# 클라이언트 측 폴링 간격 제어
REACT_QUERY_REFETCH_INTERVAL=600000
SYSTEM_STATUS_POLLING_DISABLED=true
HEALTH_CHECK_POLLING_DISABLED=true

# 스케줄러 비활성화
UNIFIED_METRICS_DISABLED=true
KEEP_ALIVE_SCHEDULER_DISABLED=true
SERVER_DATA_SCHEDULER_DISABLED=true

# 사용법:
# 1. Vercel 대시보드 → Settings → Environment Variables
# 2. 위 변수들을 Production 환경에 추가
# 3. 재배포 (vercel --prod)

# 🎯 예상 효과 (업데이트):
# - Edge Request 사용량 98% 감소 (100K → 2K)
# - 클라이언트 폴링: 10초 → 10분 간격
# - 서버 스케줄러: 20초 → 10분-1시간 간격
# - API Rate Limiting: 1분당 10회 제한
# - 5분 CDN 캐싱으로 중복 요청 완전 차단 