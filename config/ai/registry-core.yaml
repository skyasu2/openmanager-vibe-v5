# AI Registry Core - Minimal Configuration (SSOT)
# 핵심 정보만 포함, 상세 내역은 별도 문서 참조
# 목적: 토큰 사용량 최소화 (Cache Read 79% → 30% 목표)

version: "2.5.0"  # registry-core 버전 (v2.5.0: MCP 정리 - memory/time/shadcn-ui/sequential-thinking 제거, figma 추가)
last_updated: "2025-12-11"
project: "OpenManager VIBE v5.80.0"

# ============================================================================
# 1. Current Active AI Tools (현재 활성화된 AI 도구만)
# ============================================================================
ai-tools:
  claude-code:
    current_version: "v2.0.62"
    status: "✅ Active"
    role: "Interactive development (NOT for auto code review)"

  codex:
    current_version: "v0.69.0"
    wrapper_version: "v3.2.0"
    status: "✅ Active"
    role: "Code review - 3-AI rotation (codex→gemini→qwen)"

  gemini-cli:
    current_version: "v0.20.0"
    wrapper_version: "v3.2.0"
    status: "✅ Active"
    role: "Code review - 3-AI rotation (codex→gemini→qwen)"

  qwen-cli:
    current_version: "v0.4.0"
    wrapper_version: "v3.2.0"
    status: "✅ Active"
    role: "Code review - 3-AI rotation (codex→gemini→qwen)"

  kiro-cli:
    current_version: "v1.20.0"
    status: "✅ Active (Manual Usage)"
    role: "Terminal multi-agent orchestrator (chat/agent/doctor/settings)"

# 상세 정보: config/ai/changelog.yaml 참조

# ============================================================================
# 2. Active Subagents (활성화된 서브에이전트만)
# ============================================================================
subagents:
  active:
    - code-review-specialist            # HIGH - 직접 코드 리뷰 + 리뷰 결과 분석
    - architecture-specialist           # HIGH - 아키텍처 설계, 모듈화, 구조 리팩토링
    - debugger-specialist               # HIGH - 버그 분석
    - security-specialist               # CRITICAL - 보안 감사
    - test-automation-specialist        # HIGH - Vitest + Playwright
    - performance-specialist            # HIGH - Core Web Vitals, 번들 최적화
    - ui-ux-specialist                  # HIGH - UI 컴포넌트, UX 엔지니어링 (React 19 대응)
    - documentation-manager             # MEDIUM - 문서 관리

  # archived 섹션 삭제됨 (2025-12-10)
  # 삭제된 서브에이전트: dev-environment-manager, structure-refactor-specialist,
  # gcp-cloud-functions-specialist, vercel-platform-specialist, database-administrator

# 상세 정보: .claude/agents/*.md 참조
# Workflow Integration (v6.9.1):
#   - post-commit hook triggers auto-ai-review.sh
#   - Primary rotation: codex → gemini → qwen (3-AI 1:1:1 순환)
#   - Fallback chain: 각 AI가 다른 두 AI로 순차 폴백
#     - codex 실패 → gemini → qwen
#     - gemini 실패 → qwen → codex
#     - qwen 실패 → codex → gemini
#   - v6.9.1 (2025-12-08): set_last_ai 버그 수정 - 성공한 AI만 저장
#   - v6.9.0 (2025-12-08): Claude 제거 - Claude Code 세션 내 자기 호출 불가
#   - Legacy 3-AI verification system is deprecated (see archive/deprecated/3-ai-system/)

# ============================================================================
# 3. Active MCP Servers (활성화된 MCP 서버만)
# ============================================================================
mcp-servers:
  active:  # 9/9 연결 (2025-12-11, MCP 정리 완료)
    - vercel          # @open-mcp/vercel, 배포 관리 (무제한)
    - supabase        # @supabase/mcp-server-supabase, DB 관리 (무제한)
    - serena          # 코드 검색/메모리 (심볼, 구조 분석) (무제한)
    - context7        # 라이브러리 공식 문서 조회 (무제한)
    - playwright      # E2E 테스트 자동화 (무제한)
    - figma           # Design-to-Code (HTTP 원격, **6회/월 Starter**)
    - github          # GitHub 저장소 관리 (무제한)
    - tavily          # 웹 검색 - 심층 리서치 (1,000/월)
    - brave-search    # 웹 검색 - 팩트체크/버전확인 (2,000/월)

  removed:  # 2025-12-11 정리
    - filesystem      # Claude Code 내장 도구와 100% 중복
    - memory          # Serena write_memory/read_memory로 대체
    - time            # 사용 빈도 낮음, 대안 존재
    - shadcn-ui       # Context7로 문서 조회 가능
    - sequential-thinking  # Claude 자체 추론 + TodoWrite로 대체

# 역할 구분:
#   - serena: 코드 검색 (심볼, 구조 분석)
#   - github: 저장소 관리 (PR, Issues)
# 상세 정보: .mcp.json 참조

# ============================================================================
# 4. Claude Code Skills (Phase 1.3)
# ============================================================================
skills:
  active:
    - lint-smoke              # 62% token savings (v1.1.0)
    - next-router-bottleneck  # 75% token savings (v1.0.0)
    - ai-report-export        # 78% token savings (v1.1.0)
    - playwright-triage       # 77% token savings (v1.1.0)
    - security-audit-workflow # 70% token savings (v1.0.0)
    - validation-analysis     # Post-commit validation (v1.0.0)
    - ai-code-review          # 70% token savings (v1.0.0) 🆕

  average_efficiency: "71.9%"
  status: "✅ Phase 1.3 Complete (7 Skills)"

# 상세 정보: .claude/skills/*/*.md 참조

# ============================================================================
# 5. Quick Reference (빠른 참조만)
# ============================================================================
quick_commands:
  mcp_health: "./scripts/mcp/mcp-health-check.sh"
  codex_review: 'codex exec "[review request]"'
  token_usage: "npx ccusage@latest"

maintenance_frequency:
  mcp_health: "주 1회 (월요일)"
  ai_tools_health: "필요 시 (Claude Code 직접 확인)"
  docs_sync: "월 1회"

# ============================================================================
# 6. Documentation References (문서 참조)
# ============================================================================
docs:
  main: "CLAUDE.md"
  subagents: "docs/development/ai/claude-code/subagents-complete-guide.md"
  mcp_guide: "docs/environment/tools/mcp/mcp-priority-guide.md"
  changelog: "config/ai/changelog.yaml"

# End of AI Registry Core
