# AI Registry Core - Minimal Configuration (SSOT)
# 핵심 정보만 포함, 상세 내역은 별도 문서 참조
# 목적: 토큰 사용량 최소화 (Cache Read 79% → 30% 목표)

version: "2.0.1"  # registry-core 버전
last_updated: "2025-12-01"
project: "OpenManager VIBE v5.80.0"

# ============================================================================
# 1. Current Active AI Tools (현재 활성화된 AI 도구만)
# ============================================================================
ai-tools:
  claude-code:
    current_version: "v2.0.55"
    status: "✅ Active"

  codex:
    current_version: "v0.63.0"
    wrapper_version: "v2.5.0"
    status: "✅ Active"
    role: "Code review and verification"

  gemini-cli:
    current_version: "v0.18.4"
    status: "✅ Active"
    role: "Code review and verification (fallback)"

  qwen-cli:
    current_version: "v0.3.0"
    status: "✅ Active"
    role: "Code review and verification (fallback)"

  kiro-cli:
    current_version: "v1.20.0"
    status: "🆕 Beta (Not Installed)"
    role: "Terminal multi-agent orchestrator (chat/agent/doctor/settings)"

# 상세 정보: config/ai/changelog.yaml 참조

# ============================================================================
# 2. Active Subagents (활성화된 서브에이전트만)
# ============================================================================
subagents:
  active:
    - codex-specialist                  # HIGH - Code review (CLI wrapper)
    - code-review-specialist            # HIGH - 통합 코드 품질 검토
    - database-administrator            # HIGH - Supabase 전문
    - vercel-platform-specialist        # HIGH - 배포 관리
    - debugger-specialist               # HIGH - 버그 분석
    - security-specialist               # CRITICAL - 보안 감사
    - test-automation-specialist        # HIGH - Vitest + Playwright
    - documentation-manager             # MEDIUM - 문서 관리
    - ui-ux-specialist                  # MEDIUM - UI 컴포넌트
    - performance-specialist            # HIGH - Core Web Vitals, 번들 최적화 🆕

  archived:  # 2025-12-01 최적화로 아카이브
    - dev-environment-manager           # Claude 기본 기능으로 충분
    - structure-refactor-specialist     # code-review에 통합
    - gcp-cloud-functions-specialist    # Vercel이 주 플랫폼

# 상세 정보: .claude/agents/*.md 참조
# Workflow Integration:
#   - post-commit hook triggers auto-ai-review.sh (Codex → Gemini fallback, background)
#   - Claude Code analyzes generated review files from logs/code-reviews/
#   - Legacy 3-AI verification system is deprecated (see archive/deprecated/3-ai-system/)

# ============================================================================
# 3. Active MCP Servers (활성화된 MCP 서버만)
# ============================================================================
mcp-servers:
  active:  # 11/11 연결 완료 (2025-12-01)
    - vercel          # @open-mcp/vercel, 배포 관리
    - supabase        # @supabase/mcp-server-supabase, DB 관리
    - serena          # 코드 검색 전문 (심볼, 구조 분석)
    - context7        # 문서 조회
    - playwright      # E2E 테스트
    - shadcn-ui       # UI 컴포넌트
    - memory          # 메모리 관리
    - time            # 시간 유틸리티
    - sequential-thinking  # 순차적 사고
    - filesystem      # 파일 시스템 작업
    - github          # GitHub 저장소 관리 🆕

# 역할 구분:
#   - serena: 코드 검색 (심볼, 구조 분석)
#   - filesystem: 파일 작업 (읽기/쓰기, 디렉토리 탐색)
#   - github: 저장소 관리 (PR, Issues)
# 상세 정보: .mcp.json 참조

# ============================================================================
# 4. Claude Code Skills (Phase 1.1)
# ============================================================================
skills:
  active:
    - lint-smoke              # 62% token savings (v1.1.0)
    - next-router-bottleneck  # 75% token savings (v1.0.0)
    - ai-report-export        # 78% token savings (v1.1.0)
    - playwright-triage       # 77% token savings (v1.1.0)
    - security-audit-workflow # 70% token savings (v1.0.0 NEW)

  average_efficiency: "71.8%"
  status: "✅ Phase 1.1 Complete (5 Skills)"

# 상세 정보: .claude/skills/*/*.md 참조

# ============================================================================
# 5. Quick Reference (빠른 참조만)
# ============================================================================
quick_commands:
  mcp_health: "./scripts/mcp/mcp-health-check.sh"
  codex_review: 'codex exec "[review request]"'
  token_usage: "npx ccusage@latest"

maintenance_frequency:
  mcp_health: "주 1회 (월요일)"
  ai_tools_health: "필요 시 (dev-environment-manager 사용)"
  docs_sync: "월 1회"

# ============================================================================
# 6. Documentation References (문서 참조)
# ============================================================================
docs:
  main: "CLAUDE.md"
  subagents: "docs/ai/subagents-complete-guide.md"
  mcp_guide: "docs/claude/environment/mcp/mcp-priority-guide.md"
  changelog: "config/ai/changelog.yaml"

# End of AI Registry Core
