# 테스트 개선 계획

**작성일**: 2025-11-19  
**목적**: Feature Cards 업데이트 및 전체 테스트 커버리지 개선

---

## 📊 현재 테스트 현황

### 테스트 파일 수

- **E2E Tests**: 34개
- **Unit Tests**: 22개
- **Component Tests**: 3개
- **총계**: 59개

### 실행 가능한 테스트 명령어

```bash
npm run test:quick           # 빠른 유닛 테스트 (64개 통과)
npm run test:vercel:e2e      # Vercel 프로덕션 E2E
npm run test:e2e:critical    # 핵심 E2E 테스트
npm run test:comprehensive   # 전체 사용자 플로우
```

---

## 🎯 개선 필요 영역

### 1. Feature Cards 테스트 추가 (신규)

**현재 상태**: Feature Cards 관련 E2E 테스트 없음

**추가 필요 테스트**:

- [ ] 메인 페이지 4개 카드 렌더링 확인
- [ ] 각 카드 클릭 시 모달 오픈 확인
- [ ] 모달 내용 검증 (최신 업데이트 반영)
- [ ] 기술 스택 버전 확인 (Next.js 15.5.5, TypeScript 5.7.3)
- [ ] Vibe Coding 워크플로우 확인 (Claude Code + Codex/Gemini)

### 2. AI 어시스턴트 테스트 강화

**현재 상태**: 기본 테스트 존재하나 5개 기능 개별 검증 부족

**추가 필요 테스트**:

- [ ] 자연어 질의 기능
- [ ] 자동장애 보고서 생성
- [ ] 이상감지/예측 워크플로우
- [ ] AI 고급관리 기능
- [ ] 무료 티어 모니터 표시

### 3. 성능 테스트 추가

**현재 상태**: 성능 관련 테스트 부족

**추가 필요 테스트**:

- [ ] 평균 응답 시간 152ms 검증
- [ ] StaticDataLoader 성능 확인
- [ ] 캐시 히트율 측정

---

## 📝 우선순위별 작업 계획

### 🔴 높음 (즉시)

1. **Feature Cards E2E 테스트 생성**
   - 파일: `tests/e2e/feature-cards.spec.ts`
   - 내용: 4개 카드 렌더링, 모달 오픈, 최신 내용 검증

2. **Vercel 프로덕션 테스트 실행**
   - 명령어: `npm run test:vercel:e2e`
   - 목적: 실제 배포 환경 검증

### 🟡 중간 (1주 내)

3. **AI 기능별 개별 테스트**
   - 5개 AI 기능 각각 테스트 케이스 작성

4. **성능 벤치마크 테스트**
   - 응답 시간, 메모리 사용량 측정

### 🟢 낮음 (선택)

5. **시각적 회귀 테스트**
   - Playwright 스크린샷 비교

6. **접근성 테스트**
   - axe-core 통합

---

## 🚀 즉시 실행 가능한 작업

### Step 1: Feature Cards 테스트 생성

```typescript
// tests/e2e/feature-cards.spec.ts
test('메인 페이지 Feature Cards 렌더링', async ({ page }) => {
  await page.goto('/');

  // 4개 카드 확인
  await expect(page.locator('text=🧠 AI 어시스턴트')).toBeVisible();
  await expect(page.locator('text=🏗️ 클라우드 플랫폼 활용')).toBeVisible();
  await expect(page.locator('text=💻 기술 스택')).toBeVisible();
  await expect(page.locator('text=🔥 Vibe Coding')).toBeVisible();
});

test('기술 스택 카드 최신 버전 확인', async ({ page }) => {
  await page.goto('/');
  await page.click('text=💻 기술 스택');

  // 최신 버전 확인
  await expect(page.locator('text=Next.js 15.5.5')).toBeVisible();
  await expect(page.locator('text=TypeScript 5.7.3')).toBeVisible();
});

test('Vibe Coding 워크플로우 확인', async ({ page }) => {
  await page.goto('/');
  await page.click('text=🔥 Vibe Coding');

  // 업데이트된 워크플로우 확인
  await expect(page.locator('text=Claude Code (메인 개발)')).toBeVisible();
  await expect(page.locator('text=Codex CLI (코드 리뷰)')).toBeVisible();
  await expect(page.locator('text=Gemini CLI (코드 리뷰)')).toBeVisible();
});
```

### Step 2: Vercel 프로덕션 테스트

```bash
npm run test:vercel:e2e
```

---

## 📈 예상 개선 효과

### Before

- Feature Cards 테스트: 0개
- AI 기능 개별 테스트: 부족
- 프로덕션 검증: 수동

### After

- Feature Cards 테스트: 10+ 케이스
- AI 기능 개별 테스트: 5개 기능 완전 커버
- 프로덕션 검증: 자동화

---

## 🎯 다음 단계

1. Feature Cards E2E 테스트 생성
2. Vercel 프로덕션 테스트 실행
3. 테스트 결과 분석 및 개선
4. CI/CD 파이프라인 통합

---

**작성자**: AI Assistant  
**검토 필요**: Feature Cards 테스트 우선 생성
