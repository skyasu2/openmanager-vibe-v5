{
  "name": "mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for OpenManager Vibe V5",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'MCP Server - Build completed successfully!' && node --check server.js",
    "dev": "node --watch server.js",
    "health": "node health-check.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server-filesystem": "^0.6.2",
    "@modelcontextprotocol/server-github": "^0.6.2",
    "@modelcontextprotocol/sdk": "^0.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
