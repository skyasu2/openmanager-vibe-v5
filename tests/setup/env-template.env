# 🧪 OpenManager Vibe v5 - 테스트 환경 변수
# 실제 서비스 연동 테스트용 설정

# ========================================
# 🗄️ Supabase 데이터베이스 설정 (테스트용)
# ========================================
SUPABASE_URL=YOUR_SUPABASE_URL_PLACEHOLDER
NEXT_PUBLIC_SUPABASE_URL=YOUR_SUPABASE_URL_PLACEHOLDER
NEXT_PUBLIC_SUPABASE_ANON_KEY=SENSITIVE_INFO_REMOVED
SUPABASE_SERVICE_ROLE_KEY=SENSITIVE_INFO_REMOVED

# ========================================
# 🤖 Cloud Run AI 설정 (Mistral via Cloud Run)
# ========================================
CLOUD_RUN_AI_ENABLED=true
# Note: Cloud Run AI uses GCP IAM authentication, no API key needed
# CLOUD_RUN_AI_URL is configured in deployment

# ========================================
# 🔧 Claude Code MCP 서버 (개발 도구)
# ========================================
# 중요: Claude Code MCP는 stdio(표준입출력)로 통신하며 포트를 사용하지 않음
# MCP_SERVER_URL 환경변수는 사용되지 않음 (잘못된 설정)
# MCP 서버들은 Claude Code CLI가 직접 관리
# 🎯 MCP는 로컬 개발 도구 (11개 서버: filesystem, memory, github 등)
MCP_SERVER_TYPE=stdio-based

# ========================================
# 🚀 GCP AI 백엔드 설정 (MCP와 무관)
# ========================================
GCP_AI_BACKEND_URL=http://YOUR_GCP_VM_IP:3001
# AI 백엔드는 MCP 서버와 완전히 별개의 시스템



# ========================================
# 🔧 테스트 환경 설정
# ========================================
NODE_ENV=testing
TEST_TIMEOUT=15000
SKIP_EXTERNAL_TESTS=false
SKIP_ENV_VALIDATION=true

# ========================================
# 📊 모니터링 설정
# ========================================
MONITORING_ENABLED=true
METRICS_ENABLED=true
PERFORMANCE_TRACKING=true

# ========================================
# 🗃️ 데이터베이스 설정 (테스트용)
# ========================================
# Redis/Supabase 연결은 기본 환경변수 사용
# TEST_DATABASE_URL=... (필요시 추가) 