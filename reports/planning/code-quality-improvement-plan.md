# 코드 품질 개선 계획서

> 작성일: 2026-01-10
> 버전: v5.84.3
> 상태: 분석 완료, 개선 대기

---

## 1. 분석 요약

### 1.1 수정된 발견 사항

| 항목 | 초기 분석 | 실제 검토 결과 | 개선 필요 |
|------|----------|----------------|:---------:|
| Empty catch blocks | 95개 | **의도적 패턴** (Fallback, Edge Runtime 호환) | ❌ |
| TODO/FIXME | 43개 | **실제 3개** (나머지는 DEBUG 로그 레벨) | ✅ |
| 대형 파일 (500+줄) | 19개 | **대부분 응집력 있는 서비스** | ⚠️ 일부만 |
| `as any` | 0개 | 확인됨 | ❌ |
| 보안 취약점 | 0개 | 확인됨 | ❌ |

### 1.2 Empty Catch Blocks 분석

95개 중 대부분이 **의도적 패턴**:

```typescript
// 패턴 1: Fallback 값 반환 (PerformanceService.ts)
try {
  return JSON.parse(str);
} catch {
  return defaultValue; // 의도적 fallback
}

// 패턴 2: Edge Runtime 호환 (serverConfig.ts)
try {
  process.memoryUsage(); // Node.js only
} catch {
  // Edge Runtime에서는 지원되지 않음 - 의도적 무시
}

// 패턴 3: Cleanup 에러 무시 (pyodide-service.ts)
try {
  cleanup();
} catch {
  // Cleanup 실패는 무시해도 됨
}
```

**결론**: 현재 패턴은 TypeScript의 valid syntax이며, 에러 객체가 불필요한 경우 생략 가능. **개선 불필요**.

---

## 2. 실제 개선 대상

### 2.1 TODO 주석 해결 (3개)

| 파일 | 라인 | 내용 | 우선순위 |
|------|------|------|:--------:|
| `src/lib/ai/utils/context-compressor.ts` | 49 | LLM 기반 요약으로 업그레이드 | 🟡 낮음 |
| `src/test/ai-friendly-template.ts` | 197 | Implement test logic | 🟢 낮음 |
| `src/hooks/useAuth.ts` | 148 | 개발 완료 시 주석 해제 | 🔴 확인 필요 |

### 2.2 대형 파일 분할 검토 결과

| 파일 | 라인 | 분할 가능 | 이유 |
|------|------|:---------:|------|
| `postgres-vector-db.ts` | 868 | ❌ | 단일 클래스, 높은 응집력 |
| `supabase-rag-engine.ts` | 794 | ❌ | RAG 엔진 전체 로직 |
| `fixed-24h-metrics.ts` | 782 | ❌ | 데이터 파일 |
| `SystemChecklist.tsx` | 774 | ✅ | Debug 도구 분리 가능 |
| `ProcessManager.ts` | 758 | ⚠️ | 복잡도 높음, 신중히 검토 |
| `supervisor/route.ts` | 746 | ✅ | 헬퍼 함수 분리 가능 |

### 2.3 분할 대상 상세

#### SystemChecklist.tsx (774줄)
```
분리 가능 모듈:
├── types.ts (DebugTools, WindowWithDebug 인터페이스)
├── debug-tools.ts (디버그 유틸리티)
├── hooks/useSystemChecklist.ts (상태 관리 로직)
└── SystemChecklist.tsx (UI 컴포넌트)

예상 결과: 메인 파일 ~400줄
```

#### supervisor/route.ts (746줄)
```
분리 가능 모듈:
├── schemas.ts (Zod 스키마)
├── stream-parser.ts (_createDataStreamParserTransform)
├── cache-utils.ts (shouldSkipCache, isStatusQuery)
└── route.ts (POST 핸들러)

예상 결과: 메인 파일 ~300줄
```

---

## 3. 개선 작업 계획

### Phase 1: TODO 주석 정리 (예상 15분)

1. **useAuth.ts 확인** - `return true` 임시 코드 검토
2. **context-compressor.ts** - TODO를 이슈로 전환 또는 제거
3. **ai-friendly-template.ts** - 테스트 로직 구현 또는 파일 제거

### Phase 2: SystemChecklist 분할 (예상 30분)

1. 타입 분리 → `types.ts`
2. 디버그 도구 분리 → `debug-tools.ts`
3. 커스텀 훅 분리 → `useSystemChecklist.ts`
4. Import 경로 업데이트
5. 테스트 검증

### Phase 3: supervisor/route 분할 (예상 25분)

1. Zod 스키마 분리 → `schemas.ts`
2. 스트림 파서 분리 → `stream-parser.ts`
3. 캐시 유틸 분리 → `cache-utils.ts`
4. Import 경로 업데이트
5. API 테스트 검증

---

## 4. 리스크 평가

| Phase | 리스크 | 영향도 | 완화 전략 |
|-------|--------|:------:|----------|
| 1 | 낮음 | 낮음 | 단순 주석 정리 |
| 2 | 중간 | 중간 | 단계별 분리, 각 단계 후 테스트 |
| 3 | 중간 | 높음 | API 라우트 - 통합 테스트 필수 |

---

## 5. 성공 기준

- [ ] TypeScript 컴파일 성공
- [ ] Biome lint 통과
- [ ] 기존 테스트 모두 통과
- [ ] 빌드 성공 (43 pages)
- [ ] TODO 주석 0개 (또는 이슈화)

---

## 6. 결론

**즉시 개선 가능 항목:**
1. TODO 주석 3개 정리
2. SystemChecklist.tsx 분할
3. supervisor/route.ts 분할

**개선 불필요 항목:**
- Empty catch blocks (의도적 패턴)
- 대부분의 대형 파일 (높은 응집력)

**총 예상 소요 시간: ~70분**
