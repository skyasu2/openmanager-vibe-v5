# 🚀 AI 자동 코드 리뷰 리포트 (Engine: CODEX)

**날짜**: 2026-01-23 11-12-15
**커밋**: `af6ddf291`
**브랜치**: `main`
**AI 엔진**: **CODEX**

---

## 🔍 실시간 검증 결과 (2026-01-23 11:12:16)

```
ESLint: 자동 검증 (pre-push)
TypeScript: 자동 검증 (pre-push)
```

**검증 로그 파일**:
- ESLint: `logs/validation/`
- TypeScript: `logs/validation/`

---

## 📊 변경사항 요약

**커밋**: `af6ddf291ddf0422079fd7e721759d9fa5f9649d`
**메시지**: refactor(warmup): add centralized AI warmup with deduplication

## 📄 src/app/main/hooks/useSystemStart.tsx

```diff
diff --git a/src/app/main/hooks/useSystemStart.tsx b/src/app/main/hooks/useSystemStart.tsx
index 1ee30f923..1b030812e 100644
--- a/src/app/main/hooks/useSystemStart.tsx
+++ b/src/app/main/hooks/useSystemStart.tsx
@@ -13,6 +13,7 @@ import { useCallback, useEffect, useMemo, useState } from 'react';
 import { useSystemStatus } from '@/hooks/useSystemStatus';
 import { logger } from '@/lib/logging';
 import { useUnifiedAdminStore } from '@/stores/useUnifiedAdminStore';
+import { triggerAIWarmup } from '@/utils/ai-warmup';
 import debug from '@/utils/debug';
 import { debugWithEnv } from '@/utils/vercel-env-utils';
 
@@ -213,8 +214,8 @@ export function useSystemStart(options: UseSystemStartOptions) {
       // 카운트다운 시작
       setSystemStartCountdown(SYSTEM_START_COUNTDOWN_SECONDS);
       setIsSystemStarting(false);
-      // 🚀 AI 엔진 조기 웜업 (카운트다운 동안 백그라운드 기동)
-      triggerAIWakeup();
+      // 🚀 AI 엔진 웜업 (중복 요청 자동 방지)
+      void triggerAIWarmup('system-start-button');
       const timer = setInterval(() => {
         setSystemStartCountdown((prev) => {
           if (prev <= 1) {
@@ -345,21 +346,11 @@ export function useSystemStart(options: UseSystemStartOptions) {
     // 액션
     handleSystemToggle,
     navigateToDashboard: () => {
-      // 대시보드 직접 이동 시에도 웜업 시도
-      fetch('/api/ai/wake-up', { method: 'POST' }).catch(() => {});
+      // 대시보드 직접 이동 시에도 웜업 시도 (중복 요청 자동 방지)
+      void triggerAIWarmup('navigate-dashboard');
       router.push('/dashboard');
     },
   };
 }
 
-// AI 엔진 웜업 트리거 (Fire-and-forget)
-async function triggerAIWakeup() {
-  try {
-    await fetch('/api/ai/wake-up', { method: 'POST' });
-  } catch (error) {
-    // 웜업 실패는 사용자 경험을 막지 않음 (백그라운드 처리)
-    console.error('AI Wake-up signal failed:', error);
-  }
-}
-
 export type { StatusInfo, ButtonConfig };
```

## 📄 src/app/page.tsx

```diff
diff --git a/src/app/page.tsx b/src/app/page.tsx
index b76343540..cae152bb6 100644
--- a/src/app/page.tsx
+++ b/src/app/page.tsx
@@ -45,6 +45,7 @@ import {
   performanceTracker,
   preloadCriticalResources,
 } from '@/utils/vercel-optimization';
+import { triggerAIWarmup } from '@/utils/ai-warmup';
 
 // Phase 2: Lazy loading with skeleton (깜빡임 방지)
 const FeatureCardsGridSkeleton = () => (
@@ -115,9 +116,8 @@ function Home() {
 
     // 🚀 AI 엔진 조기 웜업 (페이지 로드 즉시 시작)
     // Cloud Run cold start가 5-10초 소요되므로 사용자가 페이지를 읽는 동안 미리 준비
-    fetch('/api/ai/wake-up', { method: 'POST' }).catch(() => {
-      // Fire-and-forget: 실패해도 사용자 경험에 영향 없음
-    });
+    // 60초 쿨다운으로 새로고침 시 중복 요청 방지
+    void triggerAIWarmup('landing-page');
 
     const mountTimer = setTimeout(() => {
       setIsMounted(true);
```

## 📄 src/app/system-boot/SystemBootClient.tsx

```diff
diff --git a/src/app/system-boot/SystemBootClient.tsx b/src/app/system-boot/SystemBootClient.tsx
index 426e4682e..05492eed1 100644
--- a/src/app/system-boot/SystemBootClient.tsx
+++ b/src/app/system-boot/SystemBootClient.tsx
@@ -19,6 +19,7 @@ import {
   useState,
 } from 'react';
 import { PAGE_BACKGROUNDS } from '@/styles/design-constants';
+import { triggerAIWarmup } from '@/utils/ai-warmup';
 import debug from '@/utils/debug';
 import { BootProgressBar } from './components/BootProgressBar';
 import { SmoothLoadingSpinner } from './components/SmoothLoadingSpinner';
@@ -108,10 +109,8 @@ export default function SystemBootClient() {
 
     debug.log('🚀 OpenManager 시스템 로딩 시작');
 
-    // 🚀 AI 엔진 웜업 요청 (병렬 실행)
-    fetch('/api/ai/wake-up', { method: 'POST' }).catch((err) =>
-      console.error('Boot wake-up failed:', err)
-    );
+    // 🚀 AI 엔진 웜업 요청 (중복 요청 자동 방지)
+    void triggerAIWarmup('system-boot');
 
     const timeouts: NodeJS.Timeout[] = [];
 
```

## 📄 src/utils/ai-warmup.ts

```diff
diff --git a/src/utils/ai-warmup.ts b/src/utils/ai-warmup.ts
new file mode 100644
index 000000000..0b4208e7c
--- /dev/null
+++ b/src/utils/ai-warmup.ts
@@ -0,0 +1,92 @@
+/**
+ * AI 엔진 웜업 유틸리티
+ *
+ * Cloud Run cold start 대응을 위한 조기 웜업 기능
+ * - 세션 내 중복 호출 방지 (60초 쿨다운)
+ * - Fire-and-forget 패턴 (실패해도 사용자 경험 미영향)
+ */
+
+const WARMUP_COOLDOWN_MS = 60_000; // 60초 쿨다운
+const WARMUP_STORAGE_KEY = 'ai_warmup_timestamp';
+
+/**
+ * 마지막 웜업 시간 조회
+ */
+function getLastWarmupTime(): number {
+  try {
+    if (typeof window === 'undefined') return 0;
+    const stored = sessionStorage.getItem(WARMUP_STORAGE_KEY);
+    return stored ? parseInt(stored, 10) : 0;
+  } catch {
+    return 0;
+  }
+}
+
+/**
+ * 웜업 시간 기록
+ */
+function setWarmupTime(): void {
+  try {
+    if (typeof window !== 'undefined') {
+      sessionStorage.setItem(WARMUP_STORAGE_KEY, Date.now().toString());
+    }
+  } catch {
+    // sessionStorage 비활성화 환경 무시
+  }
+}
+
+/**
+ * 웜업이 필요한지 확인 (쿨다운 체크)
+ */
+function needsWarmup(): boolean {
+  const lastWarmup = getLastWarmupTime();
+  return Date.now() - lastWarmup > WARMUP_COOLDOWN_MS;
+}
+
+/**
+ * AI 엔진 웜업 트리거
+ *
+ * - 60초 쿨다운 내에는 중복 호출하지 않음
+ * - Fire-and-forget: 결과 무시, 실패해도 사용자 경험 미영향
+ *
+ * @param source 호출 위치 (로깅용)
+ * @returns 실제 웜업 요청이 전송되었는지 여부
+ */
+export async function triggerAIWarmup(source?: string): Promise<boolean> {
+  // 쿨다운 체크
+  if (!needsWarmup()) {
+    if (process.env.NODE_ENV === 'development') {
+      console.debug(
+        `[AI Warmup] Skipped (cooldown) - source: ${source || 'unknown'}`
+      );
+    }
+    return false;
+  }
+
+  // 타임스탬프 먼저 기록 (중복 요청 방지)
+  setWarmupTime();
+
+  try {
+    await fetch('/api/ai/wake-up', { method: 'POST' });
+    if (process.env.NODE_ENV === 'development') {
+      console.debug(`[AI Warmup] Triggered - source: ${source || 'unknown'}`);
+    }
+    return true;
+  } catch {
+    // Fire-and-forget: 실패해도 무시
+    return true; // 요청은 시도됨
+  }
+}
+
+/**
+ * 웜업 상태 리셋 (테스트용)
+ */
+export function resetWarmupState(): void {
+  try {
+    if (typeof window !== 'undefined') {
+      sessionStorage.removeItem(WARMUP_STORAGE_KEY);
+    }
+  } catch {
+    // 무시
+  }
+}
```

---

## 🚀 AI 리뷰 결과

바로 리뷰 결과입니다. 이 변경사항만으로 “즉시 배포”는 위험하며, 아래 2건 수정이 선행돼야 합니다.

**1) Cooldown 영구 정지 가능 (세션 저장값 파손 시) — High**
- 영향: `sessionStorage` 값이 숫자가 아니면 `Date.now() - NaN` → `NaN`, 비교 결과가 `false`가 되어 웜업이 영구적으로 차단됩니다.
- 위치: `src/utils/ai-warmup.ts:15-43`
- 개선 스니펫:
```ts
function getLastWarmupTime(): number {
  try {
    if (typeof window === 'undefined') return 0;
    const stored = sessionStorage.getItem(WARMUP_STORAGE_KEY);
    const parsed = stored ? Number.parseInt(stored, 10) : 0;
    if (Number.isNaN(parsed)) {
      sessionStorage.removeItem(WARMUP_STORAGE_KEY);
      return 0;
    }
    return parsed;
  } catch {
    return 0;
  }
}
```

**2) sessionStorage 비활성 환경에서 과도한 중복 호출 — Medium**
- 영향: `sessionStorage`가 막힌 환경(프라이빗/보안 모드)에서는 쿨다운이 전혀 동작하지 않아 `landing/system-boot/system-start` 경로에서 연쇄적으로 웜업 호출이 발생합니다. 콜드 스타트 대비를 오히려 악화시키거나 레이트리밋을 유발할 수 있습니다.
- 위치: `src/utils/ai-warmup.ts:25-78`, 호출부 `src/app/page.tsx:117-121`, `src/app/system-boot/SystemBootClient.tsx:112-114`, `src/app/main/hooks/useSystemStart.tsx:217-218`
- 개선 스니펫 (메모리 fallback):
```ts
let lastWarmupMemory = 0;

function getLastWarmupTime(): number {
  try {
    if (typeof window === 'undefined') return 0;
    const stored = sessionStorage.getItem(WARMUP_STORAGE_KEY);
    const parsed = stored ? Number.parseInt(stored, 10) : 0;
    if (Number.isNaN(parsed)) return 0;
    return parsed;
  } catch {
    return lastWarmupMemory;
  }
}

function setWarmupTime(): void {
  const now = Date.now();
  try {
    if (typeof window !== 'undefined') {
      sessionStorage.setItem(WARMUP_STORAGE_KEY, now.toString());
      return;
    }
  } catch {
    // ignore
  }
  lastWarmupMemory = now;
}
```

**보안/안정성**
- 발견된 직접 취약점은 없음. `fetch('/api/ai/wake-up')`는 기존과 동일.
- 다만 현재는 “실패 시에도 쿨다운 확정” 정책이라, 네트워크 실패 시 60초 동안 재시도 불가. 의도인지 확인 필요.

**성능**
- 중복 호출 완화 의도는 좋지만, `sessionStorage` 실패 시 완전히 무력화됩니다(위 2번).

**코드 품질/아키텍처**
- 웜업 로직 중앙화는 일관성 개선.
- 모듈 자체는 클라이언트 전용 성격이므로, 향후 서버 컴포넌트에서 실수로 호출되지 않도록 경계(예: 주석/문서) 정도만 고려하면 충분.

**테스트/문서**
- 테스트 권장: `src/utils/ai-warmup.ts`에 대한 단위 테스트가 필요합니다.
  - 쿨다운 정상 동작
  - sessionStorage 비활성 시 메모리 fallback
  - 저장값 파손(NaN) 복구
- 문서 업데이트: 별도 문서 업데이트는 필수로 보이지 않음(동작 변경이 운영 문서에 기록되는 프로젝트라면 간단히 추가 권장).

**질문/가정**
- 웜업 실패 시에도 60초 쿨다운을 확정하는 정책이 의도된가요? 의도라면 OK, 아니라면 실패 시 타임스탬프 롤백 필요합니다.

**종합 평가**
- 점수: 7/10  
- 승인 여부: 조건부 승인 (위 2건 수정 및 유닛 테스트 추가 후 승인 가능)

원하시면 바로 수정 패치를 준비할까요?

---

## 📋 체크리스트

- [ ] 버그 위험 사항 확인 완료
- [ ] 개선 제안 검토 완료
- [ ] TypeScript 안전성 확인 완료
- [ ] 보안 이슈 확인 완료
- [ ] 종합 평가 확인 완료

## 🚨 오탐(False Positive) 기록

<!-- 거부/저점수가 오탐인 경우 아래에 기록 -->
<!-- 예시: - [x] limit 검증 이슈: Mock 핸들러라 실제 영향 없음 -->


---

**생성 시간**: 2026-01-23 11:13:28
**리뷰 파일**: `/mnt/d/cursor/openmanager-vibe-v5/reports/ai-review/pending/review-codex-2026-01-23-11-12-15.md`
**AI 엔진**: CODEX
