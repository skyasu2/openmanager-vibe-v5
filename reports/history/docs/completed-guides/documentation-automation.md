# 문서 자동화 시스템 가이드

**버전**: v5.87.0
**최종 업데이트**: 2026-01-13

---

## 📋 목차

1. [시스템 개요](#-시스템-개요)
2. [CHANGELOG 자동 갱신](#-changelog-자동-갱신)
3. [Pre-commit 문서 검증](#-pre-commit-문서-검증)
4. [README 유지보수 전략](#-readme-유지보수-전략)
5. [트러블슈팅](#-트러블슈팅)

---

## 🎯 시스템 개요

### 목적

- **CHANGELOG 자동화**: `standard-version`을 통한 시맨틱 버저닝 + CHANGELOG 생성
- **문서 품질 보장**: 코드 변경 시 문서/테스트 갱신 검증
- **일관된 문서화**: Conventional Commits 기반 표준화

### 핵심 컴포넌트

| 컴포넌트                         | 역할                          | 실행 시점      |
| -------------------------------- | ----------------------------- | -------------- |
| `npm run release:*`              | 버전 + CHANGELOG 자동 생성    | 릴리스 시      |
| `README.md`                      | 프로젝트 메인 문서            | 수동 유지보수  |

---

## 📝 CHANGELOG 자동 갱신

### 사용 방법

```bash
# 릴리스 및 CHANGELOG 갱신 (standard-version 사용)
npm run release:patch    # 패치 릴리스 (5.87.0 → 5.87.1)
npm run release:minor    # 마이너 릴리스 (5.87.0 → 5.88.0)
npm run release:major    # 메이저 릴리스 (5.87.0 → 6.0.0)
npm run release:dry-run  # 미리보기 (실제 변경 없음)
```

### 작동 원리

`standard-version`은 다음 과정을 자동으로 수행합니다:

1. **버전 결정**: Conventional Commits 분석
   - `feat:` → minor 버전 증가
   - `fix:` → patch 버전 증가
   - `BREAKING CHANGE:` → major 버전 증가

2. **커밋 분류** (8가지 타입):
   - `feat`: 🚀 Features (새 기능)
   - `fix`: 🐛 Bug Fixes (버그 수정)
   - `docs`: 📚 Documentation (문서)
   - `style`: 💅 Styles (스타일)
   - `refactor`: ♻️ Refactors (리팩토링)
   - `perf`: ⚡ Performance (성능)
   - `test`: 🧪 Tests (테스트)
   - `chore`: 🔧 Chores (잡무)

3. **자동 수행 작업**:
   - `package.json` 버전 업데이트
   - `CHANGELOG.md` 갱신
   - Git 태그 생성 (`v5.87.1`)
   - 커밋 자동 생성

### 출력 예시

```
✔ bumping version in package.json from 5.87.0 to 5.87.1
✔ bumping version in cloud-run/ai-engine/package.json from 5.87.0 to 5.87.1
✔ outputting changes to CHANGELOG.md
✔ committing package.json and CHANGELOG.md
✔ tagging release v5.87.1
```

### 통합 워크플로우

```bash
# 1. 작업 완료 후 릴리스
npm run release:patch  # 또는 :minor, :major

# 2. 태그와 함께 푸시 (자동 배포 트리거)
git push --follow-tags
```

---

## 🔍 Pre-commit 문서 검증

### 사용 방법

Hook은 git commit 시 자동으로 실행됩니다:

```bash
git commit -m "feat(api): 새 API 엔드포인트 추가"
# → Hook 자동 실행: 문서/테스트 검증
```

### 검증 로직

#### 1. 스테이징된 파일 분류

```bash
# 소스 코드 변경 감지
if [[ "$file" =~ ^src/ ]] && [[ ! "$file" =~ \.test\.(ts|tsx|js|jsx)$ ]]; then
  SOURCE_CHANGED=true
fi

# 테스트 파일 변경 감지
if [[ "$file" =~ \.(spec|test)\.(ts|tsx|js|jsx)$ ]]; then
  TEST_CHANGED=true
fi

# 문서 파일 변경 감지
if [[ "$file" =~ \.md$ ]] || [[ "$file" =~ ^docs/ ]]; then
  DOC_CHANGED=true
fi
```

#### 2. 경고 출력 (Soft Warning)

소스 코드 변경 시 테스트/문서 확인 권장:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  문서/테스트 검증 경고
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  소스 코드가 변경되었으나 테스트 파일이 스테이징되지 않았습니다.
   확인: tests/ 디렉토리에 관련 테스트가 있는지 확인하세요.

💡 권장 사항:
   - 테스트 추가: npm run test:super-fast
   - 릴리스 시: npm run release:patch
   - README 업데이트: 주요 변경사항 반영

계속 진행하려면 Enter를 누르세요...
```

### 커밋 차단하지 않는 이유

- **Soft Warning 정책**: 경고만 표시, 커밋 강제 차단 안 함
- **이유**:
  - 빠른 핫픽스 허용 (긴급 버그 수정)
  - 문서/테스트는 별도 PR로 추가 가능
  - 개발 생산성 저해 방지

---

## 📚 README 유지보수 전략

### README 구조 (8개 섹션)

1. **프로젝트 소개** (헤더, 뱃지)
2. **핵심 특징** (실시간 모니터링, GPU 애니메이션, AI 어시스턴트)
3. **빠른 시작** (설치 가이드)
4. **아키텍처 개요** (데이터 플로우)
5. **개발 및 테스트** (테스트 전략)
6. **학습 가치** (학습 내용)
7. **문서 링크** (상세 가이드)
8. **기여하기** (PR 프로세스)

### 업데이트 시점

| 변경 사항              | README 업데이트 필요 여부 |
| ---------------------- | ------------------------- |
| 새 주요 기능 추가      | ✅ 필수                   |
| 성능 개선 (>30%)       | ✅ 권장                   |
| 아키텍처 변경          | ✅ 필수                   |
| 버그 수정              | ❌ 불필요                 |
| 코드 리팩토링          | ❌ 불필요                 |
| 문서 파일 추가/변경    | ⚠️ 링크 확인             |

### 업데이트 가이드

#### 1. 주요 기능 추가 시

```markdown
### ⚡ GPU-Accelerated Animations

- **120fps 목표**: 55fps → 120fps (118% 성능 향상)
- **하드웨어 가속**: transform3d, will-change, cubic-bezier
- **9가지 카테고리**: 카드, 모달, 사이드바, 버튼, 로딩 등
- **접근성 지원**: prefers-reduced-motion 자동 대응

→ **[📚 성능 검증 가이드](./docs/performance/gpu-animation-validation.md)**
```

#### 2. 성능 지표 업데이트

```markdown
## 📊 구현 현황

- **TypeScript**: strict 모드 적용, 타입 오류 0개
- **응답시간**: 평균 152ms (이전: 200ms → 24% 개선) ✨
- **테스트**: E2E 98.2% 통과율
```

#### 3. 문서 링크 추가

```markdown
### 🔗 전문 문서

- **[📚 전체 문서 인덱스](./docs/README.md)** - 설계, API, 가이드
- **[⚡ GPU 애니메이션 가이드](./docs/performance/gpu-animation-validation.md)** - 성능 검증 ✨ 신규
```

---

## 🔧 트러블슈팅

### 문제 1: 릴리스 실패

**증상**:
```
✖ bumping version failed
```

**원인**: 작업 디렉토리에 uncommitted 변경사항 존재

**해결**:
```bash
# 변경사항 확인
git status

# 변경사항 커밋 또는 스태시
git stash  # 또는 git commit

# 재실행
npm run release:patch
```

### 문제 2: 커밋 메시지 형식 불일치

**증상**: CHANGELOG에 커밋이 누락됨

**원인**: Conventional Commits 형식 미준수

**해결**:
```bash
# ❌ 잘못된 형식
git commit -m "새 기능 추가"

# ✅ 올바른 형식
git commit -m "feat(api): 새 API 엔드포인트 추가"

# Conventional Commits 타입:
# feat, fix, docs, style, refactor, perf, test, chore
```

---

## 📖 참고 문서

- **[Conventional Commits](https://www.conventionalcommits.org/)** - 커밋 메시지 표준
- **[Husky](https://typicode.github.io/husky/)** - Git Hooks 관리
- **[CHANGELOG 가이드](https://keepachangelog.com/)** - CHANGELOG 작성 방법

---

**마지막 업데이트**: 2026-01-13
**작성자**: Claude Code
**버전**: 2.0.0 (standard-version 전환)
